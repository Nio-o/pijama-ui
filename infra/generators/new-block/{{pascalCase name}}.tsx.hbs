import React from 'react'

import { cn } from '@bem-react/classname'

import { forwardRef } from '../forwardRef/forwardRef'

import s from './{{pascalCase name}}.module.js'

export interface {{pascalCase name}}Props {
  children?: React.ReactNode
  /** Your props */
}

const cn{{pascalCase name}} = cn(s.{{pascalCase name}})

export const {{pascalCase name}} = forwardRef<{{pascalCase name}}Props, 'div'>(({ className, children, as: AsComponent = 'div', ...rest }, ref): JSX.Element | null => {
  return (
    <AsComponent {...rest} ref={ref} className={cn{{pascalCase name}}(null, [className])}>
      {children}
    </AsComponent>
  )
})
