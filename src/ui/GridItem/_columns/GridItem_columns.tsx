import { withBemMod } from '@bem-react/core'

import type { GridItemProps } from '../GridItem'

import s from './GridItem_columns.module.js'

export interface GridItemWith1Columns {
  columns?: '1'
}

export const with1Columns = withBemMod<GridItemWith1Columns, GridItemProps>(s.GridItem, {
  columns: '1',
})

export interface GridItemWith2Columns {
  columns?: '2'
}

export const with2Columns = withBemMod<GridItemWith2Columns, GridItemProps>(s.GridItem, {
  columns: '2',
})

export interface GridItemWith3Columns {
  columns?: '3'
}

export const with3Columns = withBemMod<GridItemWith3Columns, GridItemProps>(s.GridItem, {
  columns: '3',
})

export interface GridItemWith4Columns {
  columns?: '4'
}

export const with4Columns = withBemMod<GridItemWith4Columns, GridItemProps>(s.GridItem, {
  columns: '4',
})

export interface GridItemWith5Columns {
  columns?: '5'
}

export const with5Columns = withBemMod<GridItemWith5Columns, GridItemProps>(s.GridItem, {
  columns: '5',
})

export interface GridItemWith6Columns {
  columns?: '6'
}

export const with6Columns = withBemMod<GridItemWith6Columns, GridItemProps>(s.GridItem, {
  columns: '6',
})

export interface GridItemWith7Columns {
  columns?: '7'
}

export const with7Columns = withBemMod<GridItemWith7Columns, GridItemProps>(s.GridItem, {
  columns: '7',
})

export interface GridItemWith8Columns {
  columns?: '8'
}

export const with8Columns = withBemMod<GridItemWith8Columns, GridItemProps>(s.GridItem, {
  columns: '8',
})

export interface GridItemWith9Columns {
  columns?: '9'
}

export const with9Columns = withBemMod<GridItemWith9Columns, GridItemProps>(s.GridItem, {
  columns: '9',
})

export interface GridItemWith10Columns {
  columns?: '10'
}

export const with10Columns = withBemMod<GridItemWith10Columns, GridItemProps>(s.GridItem, {
  columns: '10',
})

export interface GridItemWith11Columns {
  columns?: '11'
}

export const with11Columns = withBemMod<GridItemWith11Columns, GridItemProps>(s.GridItem, {
  columns: '11',
})

export interface GridItemWith12Columns {
  columns?: '12'
}

export const with12Columns = withBemMod<GridItemWith12Columns, GridItemProps>(s.GridItem, {
  columns: '12',
})
